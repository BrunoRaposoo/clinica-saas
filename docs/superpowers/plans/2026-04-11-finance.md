# Finance Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implementar módulo financeiro com Dashboard, CRUD de cobranças, registro de pagamentos e indicadores

**Architecture:** Backend NestJS com Prisma, Frontend Next.js com TanStack Query, Contracts compartilhados

**Tech Stack:** NestJS, Prisma, PostgreSQL, Next.js, React Hook Form, Zod, TanStack Query

---

## Task 1: Database Schema

**Files:**
- Modify: `apps/api/prisma/schema.prisma`

- [ ] **Step 1: Adicionar entidades Charge e ChargeAudit ao schema**

Adicionar após TaskAudit:

```prisma
model Charge {
  id              String    @id @default(uuid())
  organizationId  String
  patientId       String?
  appointmentId  String?
  description     String
  amount          Decimal   @db.Decimal(10, 2)
  dueDate         DateTime
  status          String   @default("pending")
  paidAt          DateTime?
  paymentMethod   String?
  notes           String?
  createdBy       String
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  organization   Organization    @relation(fields: [organizationId], references: [id])
  patient       Patient?        @relation(fields: [patientId], references: [id], onDelete: Cascade)
  appointment   Appointment?   @relation(fields: [appointmentId], references: [id], onDelete: Cascade)
  createdByUser User           @relation(fields: [createdBy], references: [id])
  audits        ChargeAudit[]

  @@index([organizationId])
  @@index([patientId])
  @@index([appointmentId])
  @@index([status])
  @@index([dueDate])
  @@index([createdBy])
  @@map("charges")
}

model ChargeAudit {
  id          String   @id @default(uuid())
  chargeId    String
  action      String
  changes     Json?
  performedBy String
  performedAt DateTime @default(now())

  charge  Charge @relation(fields: [chargeId], references: [id], onDelete: Cascade)
  user    User   @relation(fields: [performedBy], references: [id])

  @@index([chargeId])
  @@index([performedBy])
  @@map("charge_audits")
}
```

- [ ] **Step 2: Adicionar relação inversa em Organization**

```prisma
charges Charge[]
```

- [ ] **Step 3: Adicionar relação inversa em Patient**

```prisma
charges Charge[]
```

- [ ] **Step 4: Adicionar relação inversa em Appointment**

```prisma
charges Charge[]
```

- [ ] **Step 5: Adicionar relação inversa em User**

```prisma
createdCharges  Charge[]
chargeAudits   ChargeAudit[]
```

- [ ] **Step 6: Executar prisma db push**

Run: `cd apps/api && npx prisma db push --skip-generate`

- [ ] **Step 7: Commit**

Run: `git add apps/api/prisma/schema.prisma && git commit -m "feat(finance): add Charge and ChargeAudit models"`